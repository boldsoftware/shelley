{
  "_README": "This file is managed by LazyCue (see shelley/lazycue). Do NOT edit by hand — it is the cached, machine-generated DSL for a self-healing browser test. To change behavior, edit the test description and re-run LazyCue.",
  "description": "Navigate to /new, wait for the message input (data-testid \"message-input\") to be visible, and click it so the textarea has keyboard focus (the @ file-completion menu only opens for a focused textarea). Fill the message input with \"@delta\". The file-completion menu (data-testid \"file-completion-menu\") should appear with an element with role \"option\" and aria-label \"delta.md\". Press the Escape key. The menu closes (selector \"[data-testid='file-completion-menu']\" matches 0 elements) but the textarea keeps focus and its text is unchanged: eval \"(function(){var el=document.querySelector('[data-testid=\\\"message-input\\\"]');return document.activeElement===el\u0026\u0026el.value==='@delta'?'pass':'fail:'+JSON.stringify(el.value)+' focused='+(document.activeElement===el);})()\" expecting \"pass\". Then press Enter while holding Shift (press_key \"Enter\" with modifiers \"Shift\"): with the menu dismissed, Shift+Enter inserts a newline as usual instead of completing anything, so eval \"(function(){var v=document.querySelector('[data-testid=\\\"message-input\\\"]').value;return v==='@delta'+String.fromCharCode(10)?'pass':'fail:'+JSON.stringify(v);})()\" expecting \"pass\".",
  "version": 1,
  "steps": [
    {
      "action": "navigate",
      "url": "/new"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid='message-input']",
      "timeout": "10s"
    },
    {
      "action": "click",
      "selector": "[data-testid='message-input']"
    },
    {
      "action": "fill",
      "selector": "[data-testid='message-input']",
      "value": "@delta"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid='file-completion-menu']",
      "timeout": "10s"
    },
    {
      "action": "assert_visible",
      "selector": "[role='option'][aria-label='delta.md']"
    },
    {
      "action": "press_key",
      "key": "Escape"
    },
    {
      "action": "assert_count",
      "selector": "[data-testid='file-completion-menu']",
      "count": 0
    },
    {
      "action": "eval",
      "expression": "(function(){var el=document.querySelector('[data-testid=\"message-input\"]');return document.activeElement===el\u0026\u0026el.value==='@delta'?'pass':'fail:'+JSON.stringify(el.value)+' focused='+(document.activeElement===el);})()",
      "expect": "pass"
    },
    {
      "action": "press_key",
      "key": "Enter",
      "modifiers": "Shift"
    },
    {
      "action": "eval",
      "expression": "(function(){var v=document.querySelector('[data-testid=\"message-input\"]').value;return v==='@delta'+String.fromCharCode(10)?'pass':'fail:'+JSON.stringify(v);})()",
      "expect": "pass"
    }
  ],
  "metadata": {
    "created_at": "2026-09-25T04:50:44.957210189Z",
    "hostname": "phil-dev",
    "model": "claude-sonnet-4-6",
    "input_tokens": 18254,
    "output_tokens": 1305,
    "estimated_cost_usd": 0.074337,
    "mode": "generated"
  }
}
