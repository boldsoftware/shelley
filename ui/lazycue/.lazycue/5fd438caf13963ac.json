{
  "_README": "This file is managed by LazyCue (see shelley/lazycue). Do NOT edit by hand — it is the cached, machine-generated DSL for a self-healing browser test. To change behavior, edit the test description and re-run LazyCue.",
  "description": "Navigate to /new, wait for the message input (data-testid \"message-input\") to be visible, and click it so the textarea has keyboard focus (the @ file-completion menu only opens for a focused textarea). Fill the message input with the text \"Use @cardamom\". No file NAME matches, but the file recipes.txt CONTAINS that word, so the file-completion menu (data-testid \"file-completion-menu\") should show an element with role \"option\" and aria-label \"recipes.txt\" (allow up to 15 seconds; content search follows the name search). Within that option the \".ff-snippet\" element contains the text \"secret ingredient: cardamom\" and its \"mark\" descendant has text \"cardamom\". Click that option. The token is replaced: eval \"(function(){var v=document.querySelector('[data-testid=\\\"message-input\\\"]').value;return v==='Use @recipes.txt '?'pass':'fail:'+JSON.stringify(v);})()\" expecting \"pass\".",
  "version": 1,
  "steps": [
    {
      "action": "navigate",
      "url": "/new"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid=\"message-input\"]",
      "timeout": "10s"
    },
    {
      "action": "click",
      "selector": "[data-testid=\"message-input\"]"
    },
    {
      "action": "fill",
      "selector": "[data-testid=\"message-input\"]",
      "value": "Use @cardamom"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid=\"file-completion-menu\"] [role=\"option\"][aria-label=\"recipes.txt\"]",
      "timeout": "15s"
    },
    {
      "action": "assert_text_contains",
      "selector": "[data-testid=\"file-completion-menu\"] [role=\"option\"][aria-label=\"recipes.txt\"] .ff-snippet",
      "text": "secret ingredient: cardamom"
    },
    {
      "action": "assert_text_contains",
      "selector": "[data-testid=\"file-completion-menu\"] [role=\"option\"][aria-label=\"recipes.txt\"] .ff-snippet mark",
      "text": "cardamom"
    },
    {
      "action": "click",
      "selector": "[data-testid=\"file-completion-menu\"] [role=\"option\"][aria-label=\"recipes.txt\"]"
    },
    {
      "action": "eval",
      "expression": "(function(){var v=document.querySelector('[data-testid=\"message-input\"]').value;return v==='Use @recipes.txt '?'pass':'fail:'+JSON.stringify(v);})()",
      "expect": "pass"
    }
  ],
  "metadata": {
    "created_at": "2026-09-25T04:50:50.283054813Z",
    "hostname": "phil-dev",
    "model": "claude-sonnet-4-6",
    "input_tokens": 28375,
    "output_tokens": 1367,
    "estimated_cost_usd": 0.10563,
    "mode": "generated"
  }
}
