{
  "_README": "This file is managed by LazyCue (see shelley/lazycue). Do NOT edit by hand — it is the cached, machine-generated DSL for a self-healing browser test. To change behavior, edit the test description and re-run LazyCue.",
  "description": "Navigate to /new, wait for the message input (data-testid \"message-input\") to be visible, and click it so the textarea has keyboard focus (the @ file-completion menu only opens for a focused textarea). Fill the message input with the text Read @\"My Doc (an opening double quote after the @ lets the query contain spaces; there is no closing quote). The file-completion menu (data-testid \"file-completion-menu\") should show an element with role \"option\" and aria-label \"My Document.md\". Press the Tab key. Because the path contains a space, the completed reference is wrapped in double quotes, followed by a trailing space: eval \"(function(){var v=document.querySelector('[data-testid=\\\"message-input\\\"]').value;return v==='Read @\\\"My Document.md\\\" '?'pass':'fail:'+JSON.stringify(v);})()\" expecting \"pass\". Nothing was attached (selector \"[data-testid='message-attachments']\" matches 0 elements).",
  "version": 1,
  "steps": [
    {
      "action": "navigate",
      "url": "/new"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid='message-input']",
      "timeout": "10s"
    },
    {
      "action": "click",
      "selector": "[data-testid='message-input']"
    },
    {
      "action": "fill",
      "selector": "[data-testid='message-input']",
      "value": "Read @\"My Doc"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid='file-completion-menu']",
      "timeout": "10s"
    },
    {
      "action": "wait_visible",
      "selector": "[data-testid='file-completion-menu'] [role='option'][aria-label='My Document.md']",
      "timeout": "10s"
    },
    {
      "action": "press_key",
      "key": "Tab"
    },
    {
      "action": "eval",
      "expression": "(function(){var v=document.querySelector('[data-testid=\"message-input\"]').value;return v==='Read @\"My Document.md\" '?'pass':'fail:'+JSON.stringify(v);})()",
      "expect": "pass"
    },
    {
      "action": "assert_count",
      "selector": "[data-testid='message-attachments']",
      "count": 0
    }
  ],
  "metadata": {
    "created_at": "2026-09-25T04:51:20.004682067Z",
    "hostname": "phil-dev",
    "model": "claude-sonnet-4-6",
    "input_tokens": 110966,
    "output_tokens": 2806,
    "estimated_cost_usd": 0.37498800000000004,
    "mode": "generated"
  }
}
